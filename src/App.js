import React from 'react';
import Home from './Pages/home/home.js';
import './App.css';

export default function App() {
  return (
    <div className='App'>
      <Home />
    </div>
  );
}

//set the steps initl values
//set the width of the bar
//set the value of the indicator

//set filled amount function
//define the highest limit that the value can reach (prevent overflow)
//define the amount completed value 4000/60-0

//indicator
